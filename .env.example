# ============================================================================
# Opulent Horizons MCP Gateway — Environment Configuration
# ============================================================================

# --- Property Database (PostgreSQL) ---
PGHOST=localhost
PGPORT=5432
PGUSER=opulent
PGPASSWORD=
PGDATABASE=property_db

# --- Zoho CRM ---
ZOHO_API_BASE=https://www.zohoapis.com/crm/v2

# OAuth2 auto-refresh (recommended):
#   1. Create a Self Client at https://api-console.zoho.com/
#   2. Generate a refresh token with scope: ZohoCRM.modules.ALL,ZohoCRM.settings.ALL
#   3. Fill in these three values:
ZOHO_CLIENT_ID=
ZOHO_CLIENT_SECRET=
ZOHO_REFRESH_TOKEN=
# ZOHO_TOKEN_URL=https://accounts.zoho.com/oauth/v2/token  # default, change for EU/IN/AU data centres

# Legacy static token (fallback — expires after ~1 hour):
ZOHO_ACCESS_TOKEN=

# --- Property DB (for Zoho sync server) ---
PROPERTY_DB_HOST=localhost
PROPERTY_DB_PORT=5432
PROPERTY_DB_USER=opulent
PROPERTY_DB_PASSWORD=
PROPERTY_DB_NAME=property_db

# --- MCP API Authentication ---
# Set to enable Bearer token auth on HTTP transport. Leave blank for local dev.
MCP_API_KEY=

# --- Webhook Secrets ---
TWILIO_AUTH_TOKEN=
NOTION_WEBHOOK_SECRET=

# --- Workflow Engine (n8n or Temporal) ---
WORKFLOW_WEBHOOK_URL=
N8N_WEBHOOK_URL=

# --- ElevenLabs ---
ELEVENLABS_API_KEY=

# --- Cal.com (Booking System) ---
CAL_API_KEY=
CAL_EVENT_TYPE_ID=4719355
CAL_TEAM_ID=190831
